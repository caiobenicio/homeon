version: '2'
services:
 postgres:
   image: postgres:9.6
   ports:
     - "5432:5432"
   environment:
     - POSTGRES_DB=homeon
     - POSTGRES_USER=homeon
     - POSTGRES_PASSWORD=homeon
     - MAX_CONNECTIONS=100
